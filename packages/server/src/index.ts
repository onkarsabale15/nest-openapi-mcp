/**
 * PLACEHOLDER — Phase 2 of the roadmap.
 *
 * This package will hold `McpModule.forRoot()`, the `@McpTool`/`@McpArg`/`@McpExpose`/`@McpExclude`
 * decorators, the Swagger-document discovery service, the Express/Fastify loopback dispatch
 * adapters, the AuthContextProvider interface, and the stdio/Streamable-HTTP/SSE transport
 * managers — see design doc §3.6–§3.12 for the full LLD.
 *
 * Exported today only so the package builds and the workspace wiring (tsconfig references,
 * turbo pipeline, eslint's no-restricted-paths boundary check) can be verified end-to-end before
 * any NestJS-specific code exists.
 */
export const SERVER_PACKAGE_PLACEHOLDER = true;
