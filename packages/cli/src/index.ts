/**
 * PLACEHOLDER — Phase 4 of the roadmap.
 *
 * This package will hold the `serve` command (OpenAPI doc in, proxying MCP server out, via an
 * axios-based HttpProxyDispatcher against `--base-url`) and the `generate` command (dry-run tool
 * manifest printer) — see design doc §3.13.
 */
export const CLI_PACKAGE_PLACEHOLDER = true;
